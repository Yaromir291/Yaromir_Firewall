<Window x:Class="Yaromir_Firewall_FINAL1.WhiteListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Белый список" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="ItemsList" Background="{DynamicResource BackgroundBrush}"
                 Foreground="{DynamicResource ForegroundBrush}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="AddButton" Width="80" Margin="0,0,5,0" Click="Add_Click"/>
            <Button x:Name="RemoveButton" Width="80" Margin="0,0,5,0" Click="Remove_Click"/>
            <Button x:Name="ClearButton" Width="100" Click="Clear_Click"/>
        </StackPanel>
    </Grid>
</Window>