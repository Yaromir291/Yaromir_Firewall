<Window x:Class="Yaromir_Firewall_FINAL1.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новое подключение" Height="280" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ProgramName" FontSize="18" FontWeight="Bold" Grid.Row="0"/>
        <TextBlock x:Name="ProgramPath" FontSize="12" Foreground="Gray" Grid.Row="1" Margin="0,0,0,10"/>
        <TextBlock x:Name="TargetInfo" FontSize="13" Grid.Row="2" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="AllowButton" Width="90" Margin="5" Click="Allow_Click" Background="#4CAF50" Foreground="White"/>
            <Button x:Name="BlockButton" Width="90" Margin="5" Click="Block_Click" Background="#F44336" Foreground="White"/>
            <Button x:Name="BlockAndKillButton" Width="150" Margin="5" Click="BlockAndKill_Click" Background="#D32F2F" Foreground="White"/>
        </StackPanel>

        <CheckBox x:Name="OnceCheckBox" Grid.Row="4" IsChecked="True" Margin="0,0,0,5" HorizontalAlignment="Center"/>
    </Grid>
</Window>